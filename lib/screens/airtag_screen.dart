import 'package:flutter/cupertino.dart';

class airtagScreen extends StatelessWidget {
  static const routeName = '/airtag';
  const airtagScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
